# Copy to .env and set your local values
PG_DSN=postgres://postgres:postgres@localhost:5432/order_demo?sslmode=disable

# NATS Streaming (stan) config
NATS_URL=nats://127.0.0.1:4222
NATS_CLUSTER_ID=test-cluster
NATS_CLIENT_ID=order-demo-consumer-1
NATS_SUBJECT=orders

# HTTP listen address
HTTP_ADDR=:8080
